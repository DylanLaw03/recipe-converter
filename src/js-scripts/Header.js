//Holds the Header for the app
export const Header = (props) => {
    return (
        <span className='title-span'>
            <span className="title">Dylan's Recipe Converter</span>
        </span>
    );
}